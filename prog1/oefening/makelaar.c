#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main()
{
    string invoer = get_string("");
    
    int n = strlen(invoer);
    for (int i = 0; i < n; i++)
    {
        if (strcmp())
    }
}