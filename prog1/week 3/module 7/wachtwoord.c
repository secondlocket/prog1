#include <stdio.h>
#include <cs50.h>

int sterk(string a)
{
    
}

int main ()
{
    do
    {
        string invoer = get_string("Wachtwoord? ");
    }
    while (sterk(invoer) == 1);
    
    return 0;
}