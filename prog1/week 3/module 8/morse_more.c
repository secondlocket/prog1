#include <stdio.h>
#include <cs50.h>

//naar_morse functie (van dit ".", dah "-" en spaties)


int main()
{
    string rauw = get_string("Code: ");
    
    for (int i = 0; i < strlen(rauw); i++)
    {
        if (rauw[i] == ',')
        {
            if (rauw[i + 1] == ',' && rauw[i + 2] == ',' && rauw[i + 2] == ','
        }
        
    }
    
    string morse[i] = naar_morse(invoer[i]);
    
    return 0;
}

