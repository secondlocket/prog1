#include <stdio.h>
#include <cs50.h>

int main () {
    int stuks = get_int("Hoeveel stuks koop je? = ");
    float prijs = get_float("Wat is de prijs per stuk? = ");
    
    if (stuks/3 = int)
    {
        stuks/3
    }
}