#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>

int main ()
{
    int teller = 0;
    char tekst[tekst_lengte];
    int binnen = 0;
    
    printf("Tekst = ");
    fgets(tekst, tekst_lengte, stdin);
    
    for (int i = 0, tekst[i] != '\0'; i++)
    {
        
    }
    
    
    
    return 0;
}