#include <stdio.h>
#include <cs50.h>
#include <string.h>





int main()
{
    char s[];
    
    string invoer = get_string("plaintext: ");
    
    for(int i = 0; i < strlen(invoer); i++)
    {
        if int invoer[i] = 'a';
        
    }
        
        
        //uitvoer
        if (isupper(s[i]))
        {
            s[i] = tolower(s[i]);
        }
        
        if (islower(s[i]))
        {
            s[i] = toupper(s[i]);
        }
        
    }
    
    return 0;
}