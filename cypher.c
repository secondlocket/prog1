#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main()
{
    string invoer = get_string("plaintext: ");
    
    for(int i = 0; i < strlen(s); i++)
    {
        s[i] = 'a';
        
        if (isupper s[i])
    }
    
    return 0;
}