#include <stdio.h>
#include <cs50.h>
#include <string.h>

//omkeren functie
char reverse(char c)
{
    if (isupper(c))
    {
        s[i] = tolower(s[i]);
    }
        
    if (islower(s[i]))
    {
        s[i] = toupper(s[i]);
    }
}


int main()
{
    char s[];
    
    string invoer = get_string("plaintext: ");
    
    for(int i = 0; i < strlen(invoer); i++)
    {
        if int invoer[i] = 'a';
        
    }
        

    
    return 0;
}