#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main()
{
    string invoer = get_string("plaintext: ");
    
    for(int pos = 0; pos < strlen(s); pos++)
    {
        s[pos] = 'a';
    }
    
    return 0;
}